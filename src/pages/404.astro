---
import BaseLayout from "../layouts/BaseLayout.astro";

const title = "ページが見つかりません | MinnaCircle";
const description = "お探しのページは存在しないか、移動した可能性があります。検索またはトップページから目的の情報へお進みください。";
---

<BaseLayout title={title} lang="ja" meta_description={description}>
  <section class="max-w-5xl mx-auto px-4 py-16 md:py-24 text-center">
    <div class="inline-block bg-white rounded-2xl shadow p-8 md:p-12">
      <p class="text-xs tracking-widest text-indigo-600 font-semibold mb-2">ERROR</p>
      <h1 class="text-3xl md:text-5xl font-extrabold mb-3">404 Not Found</h1>
      <p class="text-gray-600 md:text-lg">
        お探しのページは見つかりませんでした。URLが正しいかご確認ください。
      </p>

      <!-- クイック検索フォーム（GET /search） -->
      <form action="/search" method="get" class="mt-8 grid grid-cols-1 md:grid-cols-[1fr_180px_auto] gap-2 items-center text-left">
        <label class="sr-only" for="q">キーワード</label>
        <input
          id="q"
          name="q"
          type="search"
          placeholder="例）バスケ / 英会話 / 清澄白河"
          class="w-full rounded-xl border px-4 py-2"
        />
        <label class="sr-only" for="type">種別</label>
        <select id="type" name="type" class="rounded-xl border px-3 py-2">
          <option value="all" selected>すべて</option>
          <option value="event">イベント</option>
          <option value="circle">サークル</option>
        </select>
        <button type="submit" class="rounded-xl bg-indigo-600 text-white px-5 py-2 shadow hover:bg-indigo-700 transition">
          検索
        </button>
      </form>

      <div class="mt-6 flex flex-wrap gap-3 justify-center">
        <a href="/" class="bg-indigo-50 text-indigo-700 px-5 py-2 rounded-xl hover:bg-indigo-100 transition">トップへ戻る</a>
        <a href="/about" class="text-indigo-700 underline px-3 py-2">サービス概要（/about）</a>
        <a href="/contact" class="text-indigo-700 underline px-3 py-2">お問い合わせ（/contact）</a>
      </div>
    </div>

    <!-- サジェスト -->
    <div class="mt-10 text-sm text-gray-600">
      <p>ヒント：URLのスペルを確認するか、上の検索フォームをご利用ください。</p>
    </div>
  </section>
</BaseLayout>
