---
import BaseLayout from "../../layouts/BaseLayout.astro";
const title = "サークルを作る";
const desc = "サークルの基本情報を入力して登録申請します。";
---
<BaseLayout title={`MinnaCircle | ${title}`} lang="ja" description={desc}>
  <section class="max-w-4xl mx-auto py-10 px-4">
    <h1 class="text-3xl font-bold mb-6">{title}</h1>
    <p class="text-gray-600 mb-8">
      まずはサークルの基本情報を入力してください（このフォームはデモです）。
    </p>

    <form method="POST" action="/api/vendor/submit" class="space-y-6">
      <input type="hidden" name="lang" value="ja" />

      <div>
        <label for="circleName" class="block text-sm font-medium text-gray-700 mb-1">サークル名</label>
        <input
          id="circleName"
          name="circleName"
          type="text"
          required
          placeholder="例）ミナサー登山部"
          class="w-full rounded-md border px-3 py-2 focus:outline-none focus:ring"
        />
      </div>

      <div>
        <label for="description" class="block text-sm font-medium text-gray-700 mb-1">紹介文</label>
        <textarea
          id="description"
          name="description"
          required
          placeholder="活動内容や募集条件などを書いてください（10文字以上）"
          rows="6"
          class="w-full rounded-md border px-3 py-2 focus:outline-none focus:ring"
        ></textarea>
      </div>

      <div>
        <label for="email" class="block text-sm font-medium text-gray-700 mb-1">
          連絡先メール（任意）
        </label>
        <input
          id="email"
          name="email"
          type="email"
          placeholder="your@email.com"
          class="w-full rounded-md border px-3 py-2 focus:outline-none focus:ring"
        />
      </div>

      <!-- エラー表示（簡易版） -->
      {
        Astro.url.searchParams.has("err") && (
          <p class="text-red-600 text-sm">
            入力内容に不備があります（{Astro.url.searchParams.get("err")}）。
          </p>
        )
      }

      <div class="flex gap-3">
        <button
          type="submit"
          class="px-5 py-2 rounded-md bg-black text-white hover:opacity-90"
        >
          送信する
        </button>

        <a href="/" class="px-5 py-2 rounded-md border hover:bg-gray-50">
          ホームへ戻る
        </a>
      </div>
    </form>
  </section>
</BaseLayout>
